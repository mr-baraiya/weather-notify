# Cron Jobs

Local
- Run the app: npm run dev
- Run the cron worker: npm run cron

Vercel
- This project uses vercel.json to schedule /api/send-alert every hour.
- Deploy the project and confirm the cron job in Vercel settings.
