# Deployment on Vercel

Steps
1. Push the repository to GitHub.
2. Create a new project in Vercel and import the repo.
3. Add environment variables from docs/ENV.md.
4. Deploy.

WhatsApp Webhook
- Use the deployed URL for Twilio webhook endpoints.
- Example: https://your-app.vercel.app/api/whatsapp/webhook
