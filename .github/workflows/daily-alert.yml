name: Daily Weather Alert
on:
  schedule:
    - cron: '30 0 * * *'   # 6 AM IST = 00:30 UTC
jobs:
  send-alert:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger alert
        run: curl https://weather-notify-tau.vercel.app/api/send-alert
